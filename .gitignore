# Build results
[Bb]in/
[Oo]bj/
artifacts/
dist/
*.binlog

# Visual Studio / Rider / VS Code
.vs/
.idea/
*.user
*.suo
*.userprefs
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Test results
[Tt]est[Rr]esult*/
*.trx
*.coverage
coverage*.json
coverage*.xml
coverage*.info

# NuGet
*.nupkg
*.snupkg
packages/
!scripts/packages/

# Dynamo package output
*.dyn.bak

# OS
Thumbs.db
Desktop.ini
.DS_Store

# Local overrides
*.local.props
*.local.json

# What the API-surface test actually found, written beside the snapshot on a mismatch so the
# difference can be diffed. The snapshot itself is checked in; this is the scratch half.
tests/Interlude.Tests/api-surface.actual.txt

# MCP servers wired up for local development. .mcp.json is normally checked in so a team shares
# its tooling, but the ones used here point at a Dynamo instance on this machine's localhost and
# are no use to anybody else.
.mcp.json
