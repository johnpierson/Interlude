## In Depth

`Layout.Markdown(text)`

A block of Markdown. Supports headings, bold, italic, inline code, links, bullet and numbered lists, and horizontal rules.

The inputs are:

- `text` (_string_) — The Markdown source.

Returns `element` — The form element.

Search terms: `markdown`, `rich text`, `formatted`, `documentation`.
